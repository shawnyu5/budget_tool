import { Page } from "@playwright/test";

/** Runs goes through the login flow. Assumes we are on the login page */
// export async function loginFlow(page: Page) {
//   const userNameField = page.locator("#username");
//   const passwordField = page.locator("#password");

//   await userNameField.fill("test");
//   await passwordField.fill("test");

//   const loginButton = page.locator("button.submit");
//   await loginButton.click();
// }

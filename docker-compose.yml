services:
  budget_tool_backend:
    container_name: budget_tool_backend
    build: ./backend
    image: ghcr.io/shawnyu5/budget_tool_backend:latest
    ports:
      - "3000:80"
    env_file:
      - ./backend/.env

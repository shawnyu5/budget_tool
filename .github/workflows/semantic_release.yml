name: Semantic release
on:
  push:
    branches:
      - "master"
      - "ci/semantic_release"
      - "**"
jobs:
  semantic_release:
    name: Semantic release
    runs-on: self-hosted
    steps:
      - name: Semantic release dry run
        id: dry_run
        uses: codfish/semantic-release-action@v3
        with:
          dry-run: true
          tag-format: "v${version}"
          branches: |
            [
              {
                "name": "ci/semantic_release",
                prerelease: true
              }
            ]

      - name: Check output
        run: |
          echo ${{ steps.dry_run.outputs.release-version }}
